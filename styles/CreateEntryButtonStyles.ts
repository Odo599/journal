import {StyleSheet} from "react-native";

const CreateEntryButtonStyles = StyleSheet.create({
    container: {
        height: 70,
        width: 70,
        backgroundColor: "purple",
        position: "absolute",
        bottom: 15,
        right: 15,
        borderRadius: 20,
        alignItems: "center",
        justifyContent: "center"
    }
});

export default CreateEntryButtonStyles;